repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.6
    hooks:
      - id: ruff
        files: ^(backend|couple_simulator_engine)/
        args: [--fix]
      - id: ruff-format
        files: ^(backend|couple_simulator_engine)/

  - repo: local
    hooks:
      - id: frontend-eslint
        name: frontend eslint
        entry: docker compose run --rm --no-deps -T frontend npm run lint
        language: system
        files: ^frontend/src/.*\.(ts|tsx)$
        pass_filenames: false

      - id: frontend-prettier
        name: frontend prettier check
        entry: docker compose run --rm --no-deps -T frontend npm run format:check
        language: system
        files: ^frontend/
        exclude: ^frontend/package-lock\.json$
        pass_filenames: false
